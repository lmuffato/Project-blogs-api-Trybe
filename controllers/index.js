const userController = require('./userController');
const loginController = require('./loginController');
const categoryControler = require('./categoryController');

module.exports = {
  userController,
  loginController,
  categoryControler,
};
